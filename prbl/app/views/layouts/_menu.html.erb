<nav class="navbar navbar-expand-lg navbar-dark   bg-info">
  <a class="navbar-brand" href="<%= root_path %>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <% if teacher_signed_in? %>
          <a class="nav-item nav-link" href="<%= projects_path %>">My Projects</a>
          <% else %>
          <a class="nav-item nav-link" href="<%= finders_index_path %>">Search Projects <span class="sr-only">(current)</span></a>
          <a class="nav-item nav-link" href="<%= home_guest_path %>">Contact Us <span class="sr-only">(current)</span></a>

      <% end %>
      <a class="nav-item nav-link" href="<%= home_about_path %>">About Project Based Learning</a>
    </div>
  </div>

  <p class="navbar-text pull-right">

    <div class="navbar-nav">
      <% if teacher_signed_in? %>
        <a class="nav-item nav-link" href="">Logged as <%= current_teacher.name %></a>
      <%= link_to 'View profile', edit_teacher_registration_path, :class => 'nav-link' %>
      <%= link_to "Close Session", destroy_teacher_session_path, method: :delete, :class => 'nav-link'  %>
          <% else %>
          <%= link_to "Login", new_teacher_session_path, :class => 'nav-link' %>
          <%= link_to "Sign Up", new_teacher_registration_path, :class => 'nav-link' %>
      <% end %>
    </div>

</nav>
